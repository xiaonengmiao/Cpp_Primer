// read a sequence of ints and store

#include <iostream>
#include <vector>

using namespace::std;

int main()
{
  vector<string> str;
  string s;
  while (cin >> s)
  {
    str.push_back(s);
  }

  return 0;
}
